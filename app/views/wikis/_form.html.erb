<!--
<div class ="row">
    <div class="col-md-8">
        <%= form_for @wiki do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
            </div>
            <div class="form-group">
                <%= f.label :body %>
                <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
            </div>
            <% if current_user.role == 'admin' || current_user.role == 'premium' %>
                <div class="form-group">
                    <%= f.label :private, class: 'checkbox' do %>
                        <%= f.check_box :private %> Private wiki
                    <% end %>
                </div>
            <% end %>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %>
    </div>
</div>
-->
<div class="row">
  <div class="col-md-4">
    <p>Share your knowledge:</p>
    <ul>
      <li>Get as elaborate as you like</li>
      <li>Upgrade to
        <span class='glyphicon glyphicon-star' style="color: #fc0;"></span>
        Premium to be able to make Private Wikis.</li>
      <li>Public Wikis can be edited by anyone, but only its owner
          (if they are a premium user) may change its 'public' setting.</li>
      <li>Private Wikis can be edited by the owner or any other user the
        owner designates.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @wiki do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter wiki title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter wiki body" %>
        <p class="markdown">
          Blocipedia supports markdown: <b>**bold**</b> <i>_italic_</i> <code>`code`</code> >quote &nbsp;  • - bullet list<br/>
          text links: [link text](link url)<br/>
          <small>
            more about markdown at <a href="https://daringfireball.net/projects/markdown/basics">daringfireball</a>
            or <a href="https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet">markdown-here</a>.
          </small>
        </p>
      </div>
      <% if current_user.premium? && (@wiki.user == current_user || params[:action] == 'new') %>
        <div class="form-group">
          <%= f.label :public, class: 'checkbox' do %>
            <%= f.check_box :public %> Public wiki
          <% end %>
        </div>
      <% end %>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
    <% end %>
  </div>
</div>